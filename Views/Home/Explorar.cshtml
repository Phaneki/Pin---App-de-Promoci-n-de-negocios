@model IEnumerable<PinAppdePromo.Models.Business>

@section Styles {
    <link rel="stylesheet" href="~/css/explorar.css" asp-append-version="true" />
}

<div class="container-fluid mt-4">
    <div class="row">
        <aside class="col-md-3 pe-4">
            <form action="/Home/Explorar" method="get" class="card border-0 shadow-sm p-3 filtro-sidebar">
                <div class="d-flex justify-content-between align-items-center mb-3">
                    <h5 class="fw-bold mb-0">Filtros</h5>
                    <a href="/Home/Explorar" class="text-decoration-none small" style="color: var(--main-orange);">Limpiar Filtros</a>
                </div>

                <div class="mb-4">
                    <label class="form-label text-muted small fw-bold">BÚSQUEDA DIRECTA</label>
                    <div class="input-group">
                        <span class="input-group-text bg-light border-end-0"><i class="bi bi-search"></i></span>
                        <input type="text" name="busqueda" value="@Context.Request.Query["busqueda"]" class="form-control bg-light border-start-0" placeholder="Ej. Cevicheria, Taller...">
                    </div>
                </div>

                <div class="mb-4">
                    <label class="form-label text-muted small fw-bold">CATEGORÍAS</label>
                    @if (ViewBag.Categorias != null)
                    {
                        @foreach (var cat in ViewBag.Categorias)
                        {
                            var isChecked = ((List<int>)ViewBag.CategoriasSeleccionadas).Contains((int)cat.CategoryId);
                            <div class="form-check mb-2">
                                <input class="form-check-input custom-check" type="checkbox" name="categorias" value="@cat.CategoryId" id="cat_@cat.CategoryId" @(isChecked ? "checked" : "")>
                                <label class="form-check-label" for="cat_@cat.CategoryId">@cat.Name</label>
                            </div>
                        }
                    }
                </div>

                <div class="mb-4">
                    <label class="form-label text-muted small fw-bold">UBICACIÓN (LIMA)</label>
                    <select name="distrito" class="form-select bg-light">
                        <option value="">Todos los distritos</option>
                        <option value="Miraflores" selected="@(Context.Request.Query["distrito"] == "Miraflores" ? "selected" : null)">Miraflores</option>
                        <option value="San Isidro" selected="@(Context.Request.Query["distrito"] == "San Isidro" ? "selected" : null)">San Isidro</option>
                        <option value="Surco" selected="@(Context.Request.Query["distrito"] == "Surco" ? "selected" : null)">Surco</option>
                    </select>
                </div>

                <div class="mb-4">
                    <label class="form-label text-muted small fw-bold">RANGO DE PRECIO</label>
                    <div class="btn-group w-100" role="group">
                        <button type="button" class="btn btn-outline-secondary active">S/</button>
                        <button type="button" class="btn btn-outline-secondary">S/ S/</button>
                        <button type="button" class="btn btn-outline-secondary">S/ S/ S/</button>
                    </div>
                </div>
                
                <input type="hidden" name="orden" id="ordenHidden" value="@ViewBag.OrdenActual" />
                <button type="submit" class="btn w-100 fw-bold text-white mt-2 shadow-sm" style="background-color: var(--main-orange);">Aplicar Filtros</button>
            </form>
        </aside>

        <main class="col-md-9">
            <div class="d-flex justify-content-between align-items-center mb-4">
                <p class="text-muted mb-0">Mostrando <span class="fw-bold text-dark">@Model.Count() negocios</span> en total</p>
                <div class="d-flex align-items-center">
                    <span class="me-2 text-muted">Ordenar por:</span>
                    <select id="ordenSelect" class="form-select form-select-sm border-0 fw-bold" style="width: auto; color: var(--main-orange);">
                        <option value="" selected="@(ViewBag.OrdenActual == null)">Relevancia</option>
                        <option value="calificacion" selected="@(ViewBag.OrdenActual == "calificacion")">Calificación</option>
                        <option value="nombre" selected="@(ViewBag.OrdenActual == "nombre")">Nombre A-Z</option>
                    </select>
                </div>
            </div>

            <div class="row g-4">
                @foreach (var negocio in Model)
                {
                    <div class="col-md-6">
                        <div class="card h-100 border-0 shadow-sm overflow-hidden card-negocio">
                            <div class="position-relative" style="height: 200px;">
                                @if (negocio.Images != null && negocio.Images.Any())
                                {
                                    <img src="@Url.Content(negocio.Images.First().ImageUrl)" class="card-img-top" alt="@negocio.TradeName" style="height: 200px; object-fit: cover; width: 100%;" />
                                }
                                else
                                {
                                    <div class="d-flex align-items-center justify-content-center bg-secondary text-white" style="height: 200px; width: 100%;">
                                        <h4 class="fw-bold text-center m-0">@negocio.TradeName</h4>
                                    </div>
                                }

                                @if (negocio.Status == "Promoted")
                                {
                                    <span class="badge bg-white text-dark position-absolute top-0 start-0 m-3 shadow-sm px-3 py-2">DESTACADO</span>
                                }
                            </div>
                            <div class="card-body">
                                <div class="d-flex justify-content-between align-items-start">
                                    <div>
                                        <span class="small fw-bold text-uppercase" style="color: var(--main-orange);">@(negocio.Category?.Name ?? "General")</span>
                                        <h5 class="card-title fw-bold mt-1">@negocio.TradeName</h5>
                                    </div>
                                    <span class="badge bg-light text-dark border"><i class="bi bi-star-fill text-warning me-1"></i> @(negocio.Reviews != null && negocio.Reviews.Any() ? Math.Round(negocio.Reviews.Average(r => r.Rating), 1).ToString("0.0") : "5.0")</span>
                                </div>
                                <p class="card-text text-muted small mb-1"><i class="bi bi-geo-alt me-1"></i> @negocio.Address</p>
                                <p class="card-text text-success small mb-3"><i class="bi bi-clock me-1"></i> Abierto</p>
                                
                                <div class="row g-2">
                                    <div class="col-6">
                                        <a asp-action="InfNegocio" asp-route-id="@negocio.BusinessId" class="btn btn-orange-pin w-100 py-2 fw-bold">Ver perfil</a>
                                    </div>
                                    <div class="col-6">
                                       <a href="https://www.google.com/maps/search/?api=1&query=@(negocio.Latitude.ToString(System.Globalization.CultureInfo.InvariantCulture)),@(negocio.Longitude.ToString(System.Globalization.CultureInfo.InvariantCulture))" target="_blank" class="btn btn-light w-100 py-2 fw-bold border text-muted d-block text-center text-decoration-none"><i class="bi bi-map me-1"></i> Ruta</a>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                }
            </div>
            
            @if (!Model.Any())
            {
                <div class="text-center mt-5 py-5">
                    <i class="bi bi-search display-1 text-muted" style="opacity: 0.3;"></i>
                    <h4 class="mt-3 text-muted">No encontramos negocios</h4>
                    <p class="text-muted">Intenta con otros filtros o términos de búsqueda.</p>
                </div>
            }
        </main>
    </div>
</div>

@section Scripts {
<script>
    document.getElementById('ordenSelect').addEventListener('change', function() {
        // Sincronizar con el hidden input del form y enviar
        document.getElementById('ordenHidden').value = this.value;
        document.querySelector('.filtro-sidebar').submit();
    });
</script>
}